# Production Environment Variables Template for Backend
# Copy this file to .env.production and update the values

# Environment
NODE_ENV=production

# Server Configuration
PORT=10000

# MongoDB Connection (MongoDB Atlas)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/freecodeveda

# JWT Configuration
JWT_SECRET=your_secure_jwt_secret_key_here

# CORS Configuration (Your Netlify frontend URL)
CORS_ORIGIN=https://your-netlify-site.netlify.app

# Instructions:
# 1. Copy this file to .env.production
# 2. Replace 'username', 'password', 'cluster.mongodb.net' with your MongoDB Atlas details
# 3. Generate a secure JWT_SECRET (use a strong random string)
# 4. Replace 'your-netlify-site.netlify.app' with your actual Netlify frontend URL
# 5. The .env.production file is already in .gitignore and won't be committed
